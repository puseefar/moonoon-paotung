# บันทึกความก้าวหน้า App หมูนุ่น + เป๋าตุง

**เริ่มบันทึก:** 18 พฤษภาคม 2569  
**จัดทำโดย:** SEVENDOG DEV Team  
**อ้างอิงแผน:** `DEVELOPMENT-PLAN.md`

---

## Progress Log

| วันที่ | Phase | งาน | สถานะ | หมายเหตุ | ไฟล์ที่เกี่ยวข้อง |
|--------|-------|-----|-------|----------|-------------------|
| 18 พฤษภาคม 2569 | Documentation | อ่านเอกสาร Feature Inventory ฉบับเดิม และเอกสาร strategic roadmap จาก `f:\dawloadAll\feature-inventory-moonoon-paotung.md` | เสร็จแล้ว | บันทึกไฟล์ strategic roadmap เข้าโปรเจกต์แล้ว | `FEATURE-INVENTORY.md`, `STRATEGIC-FEATURE-ROADMAP.md` |
| 18 พฤษภาคม 2569 | Documentation | สร้างแผนพัฒนาแยกตาม Phase 0-3 | เสร็จแล้ว | ใช้เป็นตัวควบคุมลำดับงานก่อนเริ่ม implement | `DEVELOPMENT-PLAN.md` |
| 18 พฤษภาคม 2569 | Documentation | สร้างไฟล์บันทึกความก้าวหน้า | เสร็จแล้ว | ใช้บันทึกทุก milestone ระหว่างพัฒนา | `PROGRESS-LOG.md` |
| 18 พฤษภาคม 2569 | Phase 0 | ตรวจ baseline ของ screens/services/database เทียบเอกสาร | เสร็จแล้ว | หน้าจอหลักตรง 15 screens ตามเอกสาร; มีไฟล์เสริม `modal`, `+not-found`, `+html` และมี `app/(tabs)/report-tab.tsx`; database ตรง 8 tables; services มี 12 core services และมี OCR provider เสริม `visionOCR`, `mlkitOCR` | `app/`, `services/`, `db/schema.ts` |
| 18 พฤษภาคม 2569 | Phase 0.2 | ตรวจ TypeScript/build/runtime errors | เสร็จแล้ว | `npx.ps1` ติด PowerShell execution policy จึงตรวจซ้ำด้วย `npx.cmd tsc --noEmit`; TypeScript ผ่าน ไม่มี error | `package.json`, `tsconfig.json` |
| 18 พฤษภาคม 2569 | Phase 0.3 | ตรวจ flow หลัก: add transaction, history, report, wallet, scan slip | ยังไม่เริ่ม | ขั้นตอนถัดไป | `app/`, `services/`, `stores/` |
| 18 พฤษภาคม 2569 | Documentation | สร้างคิวดำเนินการทีละขั้นจากเอกสาร Feature Inventory & Roadmap | เสร็จแล้ว | ใช้เป็น checklist หลักสำหรับทำงานต่อและบันทึก progress ทุกระยะ | `IMPLEMENTATION-QUEUE.md`, `DEVELOPMENT-PLAN.md` |
| 18 พฤษภาคม 2569 | Phase 0.3 | เริ่มตรวจ flow หลักแบบ static + compile baseline | กำลังทำ | ตรวจพบ add transaction, history, report, wallet, scan slip เชื่อมผ่าน store/service ครบ; `npx.cmd tsc --noEmit` ผ่าน | `app/(tabs)/add.tsx`, `app/(tabs)/history.tsx`, `app/report.tsx`, `app/wallet-manage.tsx`, `app/scan-slip.tsx`, `services/` |
| 18 พฤษภาคม 2569 | Phase 0.3 | ตรวจ Baseline QA flow หลักครบ 5 เส้นทาง | เสร็จแล้ว | Add Transaction, History, Report ผ่าน static review; Wallet และ Scan Slip ผ่านแบบมี technical debt; compile ผ่านด้วย `npx.cmd tsc --noEmit` | `IMPLEMENTATION-QUEUE.md`, `app/`, `services/`, `stores/` |
| 18 พฤษภาคม 2569 | Phase 0.4 | จัดลำดับ Technical Debt ก่อนเพิ่มฟีเจอร์ | เสร็จแล้ว | ระบุ TD-01 duplicate slip unique hash, TD-02 wallet transfer overdraft, TD-03 store loading error handling, TD-04 gallery scan fallback | `IMPLEMENTATION-QUEUE.md` |
| 18 พฤษภาคม 2569 | TD-01 | แก้ duplicate slip save flow | เสร็จแล้ว | เมื่อพบสลิปซ้ำ ปุ่มบันทึกถูกปิด, handler แสดง alert กันการบันทึกซ้ำ, และ skip ไม่พยายาม insert `qrHash` เดิม; `npx.cmd tsc --noEmit` ผ่าน | `app/scan-slip.tsx`, `IMPLEMENTATION-QUEUE.md`, `DEVELOPMENT-PLAN.md` |
| 18 พฤษภาคม 2569 | TD-03 | เพิ่ม error-safe loading ให้ store หลัก | เสร็จแล้ว | ใส่ `try/finally` ให้ action ที่เปิด `isLoading` ใน transaction, wallet, summary stores เพื่อไม่ให้ loading ค้างเมื่อ service error; `npx.cmd tsc --noEmit` ผ่าน | `stores/useTransactionStore.ts`, `stores/useWalletStore.ts`, `stores/useSummaryStore.ts`, `IMPLEMENTATION-QUEUE.md` |
| 18 พฤษภาคม 2569 | TD-02 | เพิ่ม guard กันโอนเงินเกินยอด | เสร็จแล้ว | หน้าโอนเงินปิดปุ่มและแจ้งยอดเงินไม่พอเมื่อยอดต้นทางไม่พอ; service ตรวจซ้ำและ throw `Insufficient wallet balance`; `npx.cmd tsc --noEmit` ผ่าน | `app/wallet-transfer.tsx`, `services/walletService.ts`, `IMPLEMENTATION-QUEUE.md` |
| 18 พฤษภาคม 2569 | TD-04 | เพิ่ม fallback เมื่อ Gallery scan อ่าน QR/OCR ไม่เจอ | เสร็จแล้ว | reset state ก่อนอ่านรูปใหม่ และแสดง alert แนะนำเลือกรูปสลิปที่ชัดขึ้นหรือใช้กล้องสแกนแทน; `npx.cmd tsc --noEmit` ผ่าน | `app/scan-slip.tsx`, `IMPLEMENTATION-QUEUE.md` |
| 18 พฤษภาคม 2569 | Phase 1.1 | เริ่มและทำ Daily Money Snapshot บนหน้าแรก | เสร็จแล้ว | เพิ่ม service คำนวณวันนี้ใช้ไป, เดือนนี้คงเหลือ, ค่าเฉลี่ยรายวัน, หมวดรายจ่ายเด่น, รายการประจำใกล้ถึง และ insight card; เชื่อมเข้า Home refresh/focus; `npx.cmd tsc --noEmit` ผ่าน | `services/dailySnapshotService.ts`, `components/home/DailyMoneySnapshot.tsx`, `app/(tabs)/index.tsx`, `services/index.ts` |
| 18 พฤษภาคม 2569 | Phase 1.2 | เพิ่ม Quick Add Parser รอบแรก | เสร็จแล้ว | เพิ่ม rule-based parser สำหรับข้อความ เช่น `กาแฟ 45`, `เงินเดือน 30000`; หน้า Add มีช่อง Quick Add เติม amount/type/category/note เข้า form; `npx.cmd tsc --noEmit` ผ่าน | `services/quickAddParser.ts`, `app/(tabs)/add.tsx`, `services/index.ts`, `IMPLEMENTATION-QUEUE.md`, `DEVELOPMENT-PLAN.md` |
| 18 พฤษภาคม 2569 | Phase 1.3 | เพิ่ม Starter Templates สำหรับ onboarding | เสร็จแล้ว | เพิ่ม 6 persona templates: นักศึกษา, พนักงานเงินเดือน, ฟรีแลนซ์, แม่ค้าออนไลน์, เจ้าของร้าน, คนเตรียมยื่นภาษี; สร้างหมวดที่ขาดและข้ามหมวดซ้ำ; เพิ่มหน้า `starter-templates` และทางเข้าจาก Settings; `npx.cmd tsc --noEmit` ผ่าน | `services/starterTemplateService.ts`, `app/starter-templates.tsx`, `app/(tabs)/settings.tsx`, `app/_layout.tsx`, `services/index.ts` |
| 18 พฤษภาคม 2569 | Phase 1.4 | เพิ่ม Tax Readiness Checklist แบบ compliance-safe | เสร็จแล้ว | วิเคราะห์ความเสี่ยงของคำว่า Score แล้วเปลี่ยนเป็น checklist ความครบถ้วนเอกสาร, เพิ่ม disclaimer ว่าไม่ใช่คำแนะนำ/การคำนวณภาษี, เพิ่ม reminder ภ.ง.ด.94 รอบครึ่งปีสำหรับกลุ่มเงินได้ประเภท 5-8 และ ภ.ง.ด.90/91 รอบปลายปี; `npx.cmd tsc --noEmit` ผ่าน | `services/taxReadinessService.ts`, `app/tax-readiness.tsx`, `app/(tabs)/settings.tsx`, `app/_layout.tsx`, `services/index.ts`, `IMPLEMENTATION-QUEUE.md`, `DEVELOPMENT-PLAN.md` |
| 18 พฤษภาคม 2569 | Phase 1.5 | เพิ่ม Slip Inbox State Machine แบบ local-first | เสร็จแล้ว | เพิ่มสถานะ `pending`, `needs_review`, `confirmed`, `tax_evidence`, `skipped`; เพิ่มหน้า Slip Inbox สำหรับกรอง/เปลี่ยนสถานะสลิป; บันทึก product decision ว่า Free เก็บข้อมูลในเครื่องได้ใจกว้าง และ Pro ขาย sync/cloud convenience ไม่ใช่ความปลอดภัยพื้นฐาน; `npx.cmd tsc --noEmit` ผ่าน | `db/schema.ts`, `services/slipService.ts`, `services/slipInboxService.ts`, `app/slip-inbox.tsx`, `app/(tabs)/settings.tsx`, `components/home/MenuGrid.tsx`, `IMPLEMENTATION-QUEUE.md` |
| 18 พฤษภาคม 2569 | Stability/UX | ปรับ Expo Go notification guard และ Smart Quick Add | เสร็จแล้ว | เปลี่ยน notification service เป็น lazy import และปิด flow notification ใน Expo Go พร้อมข้อความแนะนำ development build; รัน `expo install --fix` ปรับ dependency ให้ตรง SDK; เพิ่ม synonym Quick Add เช่น ฟรีแลนซ์/รับงาน/ค่าจ้าง/ขายของ/ลูกค้าจ่าย และเพิ่ม preview + ปุ่มบันทึกเลย; `npx.cmd tsc --noEmit` ผ่าน, `expo-doctor` เหลือ warning เฉพาะ `@react-native-ml-kit/text-recognition` untested New Architecture | `services/notificationService.ts`, `app/notification-settings.tsx`, `services/quickAddParser.ts`, `app/(tabs)/add.tsx`, `package.json`, `package-lock.json`, `app.json` |
| 18 พฤษภาคม 2569 | Stability/UX | ปรับ Smart Entry ให้ลื่นขึ้นจากผลทดสอบมือถือ | เสร็จแล้ว | เพิ่ม normalization รองรับ `ฟรีเเลนซ์`/`ฟรีแลนซ์`, เพิ่ม keyword รายจ่ายจริง เช่น `จ่ายตลาด`, `ผัก`, `ผลไม้`, `ห้องซ้อมดนตรี`, `ค่าห้อง`; หน้า Add auto-suggest หมวดระหว่างพิมพ์และลอง parse ซ้ำก่อนเตือนเลือกหมวด; เปลี่ยน feedback หลังบันทึกเป็น snackbar เพื่อให้บันทึกต่อได้เร็วขึ้น; `npx.cmd tsc --noEmit` ผ่าน | `services/quickAddParser.ts`, `app/(tabs)/add.tsx`, `IMPLEMENTATION-QUEUE.md` |
| 18 พฤษภาคม 2569 | Stability/UX | ปรับ Thai Smart Parser จากเคส `รับค่าตัดต่อวีดีโอ` | เสร็จแล้ว | เปลี่ยนการเดา type เป็น weighted scoring ให้คำกริยา `รับค่า`, `รับเงิน`, `เงินเข้า`, `ลูกค้าโอน`, `ขายได้` มีน้ำหนักสูงกว่าคำว่า `ค่า`; เพิ่มคำงานฟรีแลนซ์ เช่น `ตัดต่อ`, `วีดีโอ`, `ทำเว็บ`, `ออกแบบ`, `โลโก้`, `ถ่ายรูป`, `แอดมินเพจ`; เปลี่ยนปุ่ม Quick Add จาก `เติม` เป็น `เพิ่ม` และปรับ placeholder ให้เข้าใจง่ายขึ้น; `npx.cmd tsc --noEmit` ผ่าน | `services/quickAddParser.ts`, `app/(tabs)/add.tsx` |

| 21 พฤษภาคม 2569 | Wallet / Audit UX | ยกระดับระบบกระเป๋าเงินหลายใบและประวัติตรวจสอบย้อนหลัง | เสร็จแล้ว | เพิ่มหน้า wallet detail, timeline ธุรกรรมต่อกระเป๋า, โอนเงินข้ามกระเป๋าแบบมี balance before/after, เพิ่ม `wallet_activity_logs`, รองรับโลโก้ธนาคารไทยหลัก, และปรับ UX หน้าการโอนให้เสถียรไม่กระโดด; `npx.cmd tsc --noEmit` ผ่าน | `app/wallet-manage.tsx`, `app/wallet/[id].tsx`, `app/wallet-transfer.tsx`, `services/walletService.ts`, `db/schema.ts`, `constants/walletBrands.ts` |
| 21 พฤษภาคม 2569 | Stability / UX | ปรับเสถียรภาพหน้าเริ่มต้นและรูปแบบ feedback กลางแอป | เสร็จแล้ว | แก้ปัญหา splash screen ค้างจาก font/db init, เพิ่ม error-safe loading ใน root/db flow, เปลี่ยนข้อความแจ้งทั่วไปเป็น snackbar โทนเดียวกัน, และแยก `ยอดรวมทุกกระเป๋า` ออกจาก `สุทธิเดือนนี้` บนหน้า Home ให้เข้าใจตรงกัน; `npx.cmd tsc --noEmit` ผ่าน | `app/_layout.tsx`, `db/provider.tsx`, `components/ui/SnackbarProvider.tsx`, `components/home/HomeHeader.tsx`, `components/home/DailyMoneySnapshot.tsx`, `app/(tabs)/index.tsx` |
| 21 พฤษภาคม 2569 | Categories / Smart Entry / Starter Templates | ขยายหมวดอาชีพไทยและทำตัวช่วยเลือก template 3 คำถาม | เสร็จแล้ว | เพิ่มหมวดรายรับ/รายจ่ายใหม่สำหรับอาชีพและกิจกรรมจริง, ปรับ Quick Add เป็น Dictionary + Scoring + User Learning ให้เดาคำอย่าง `ขายผัก`, `ขายของออนไลน์`, `รับงานเล่นดนตรี` ได้แม่นขึ้น, อัปเกรด Starter Templates เป็น 12 persona templates พร้อม search/filter และ flow `ให้ระบบช่วยเลือก` แบบถาม 3 คำถามแล้วแนะนำ template อัตโนมัติ; `npx.cmd tsc --noEmit` ผ่าน | `db/seed.ts`, `services/categoryService.ts`, `services/quickAddParser.ts`, `services/quickAddLearningService.ts`, `services/starterTemplateService.ts`, `app/starter-templates.tsx`, `STARTER-TEMPLATES-SPEC.md` |
| 21 พฤษภาคม 2569 | Smart Entry / Starter Templates | เชื่อม template bias จริงและทำ flow `สร้างชุดเริ่มต้นเอง` | เสร็จแล้ว | เพิ่ม active quick add profile ใน `app_settings`, ให้ parser รองรับ starter profile bias, หน้า Add แสดงโหมดช่วยเดาปัจจุบัน, และหน้า Starter Templates ใช้ template เดี่ยวหรือสร้างชุดผสมได้จริง; `npx.cmd tsc --noEmit` ผ่าน, `node .\\tests\\quick-add\\run-quick-add-regression.cjs --suite all` ผ่าน 251/251, และยืนยัน bias เฉพาะเคส `ลูกค้าโอน`, `รับงาน`, `เติมน้ำมัน` เพิ่มเติม | `services/appSettingsService.ts`, `services/starterTemplateService.ts`, `services/quickAddParser.ts`, `services/index.ts`, `app/(tabs)/add.tsx`, `app/starter-templates.tsx` |
| 21 พฤษภาคม 2569 | Smart Entry / Voice Amount Parsing | รองรับจำนวนเงินภาษาไทยจากการพิมพ์ด้วยเสียง | เสร็จแล้ว | เพิ่มการอ่านจำนวนเงินแบบ `4 แสนบาท`, `4แสนบาท`, `สี่แสนบาท`, `หนึ่งหมื่นห้าพันบาท` และแบบผสมเลข+ข้อความ; ปรับ learning flow ให้ตัด amount ภาษาไทยออกจาก keyword ถูกต้องขึ้น; อัปเกรด regression ให้ตรวจ `expectedAmount` จริงและผ่าน 7/7 เคสใหม่; `npx.cmd tsc --noEmit` ผ่าน, `node .\\tests\\quick-add\\run-quick-add-regression.cjs --suite all` ผ่าน 258/258 | `services/quickAddParser.ts`, `services/quickAddLearningService.ts`, `tests/quick-add/run-quick-add-regression.cjs`, `tests/quick-add/cases.th.json`, `tests/quick-add/cases.adversarial.th.json` |
| 21 พฤษภาคม 2569 | Smart Entry / Voice Intent Accuracy | แก้เคสเสียง `เสียค่า platform ไลฟ์สด 750,000 บาท` ที่เดาเป็นรายรับ | เสร็จแล้ว | เพิ่มน้ำหนักคำรายจ่ายกลุ่ม `เสียค่า`, `ค่าแพลตฟอร์ม`, `โดนหักค่า` ให้ Quick Add ตัดสิน type ได้แม่นขึ้น, ปรับ starter profile category matching ให้ยึดหมวดตรงคำก่อนหมวดโปรไฟล์, และเพิ่ม regression เคส `เสียค่า platform` กับ `โดนหักค่า platform`; `npx.cmd tsc --noEmit` ผ่าน, `node .\\tests\\quick-add\\run-quick-add-regression.cjs --suite all` ผ่าน 261/261 | `services/quickAddParser.ts`, `tests/quick-add/cases.th.json`, `tests/quick-add/cases.adversarial.th.json` |
| 22 พฤษภาคม 2569 | Smart Entry / Expense Bias | เพิ่มน้ำหนักรายจ่ายสำหรับคำต้นทุน ค่าจ้าง ค่าส่ง แพ็กของ และค่าใช้จ่ายธุรกิจจากเสียงจริง | เสร็จแล้ว | แก้ type scoring ให้คำอย่าง `ค่าจ้าง`, `เงินเดือนพนักงาน`, `ค่าส่ง`, `แพ็กของ`, `ต้นทุน`, `ค่าธรรมเนียม`, `ค่าวัตถุดิบการผลิต`, `ค่าเครื่องจักร`, `ค่าอุปกรณ์ไอที`, `คืนเงินให้ลูกค้า`, `ค่าคอมมิชชั่น creator` เอนไปทาง `expense` ชัดขึ้น, เพิ่ม intent/category rules สำหรับค่าแรงและต้นทุนดำเนินงาน, และขยาย starter template bias ของ `แม่ค้าออนไลน์` กับ `ธุรกิจส่วนตัว`; `npx.cmd tsc --noEmit` ผ่าน, `node .\\tests\\quick-add\\run-quick-add-regression.cjs --suite all` ผ่าน 279/279 | `services/quickAddParser.ts`, `services/starterTemplateService.ts`, `tests/quick-add/cases.th.json`, `tests/quick-add/cases.adversarial.th.json`, `PROGRESS-LOG.md` |
| 22 พฤษภาคม 2569 | Smart Entry / Expense Categories | เพิ่มหมวด `ค่าใช้จ่ายธุรกิจ` และ `ค่าใช้จ่ายการเกษตร` พร้อมอัปเดตผู้ใช้ template เดิมอัตโนมัติ | เสร็จแล้ว | เพิ่มหมวดใหม่ให้ template `แม่ค้าออนไลน์`, `ธุรกิจส่วนตัว`, และ `เกษตรกร`, แยกกฎ `ต้นทุนสินค้า` ออกจากค่าใช้จ่ายดำเนินงานให้ชัดขึ้น, ปรับ Quick Add ให้คำอย่าง `ค่าจ้างแอดมิน`, `ค่าธรรมเนียม`, `ค่าแพลตฟอร์ม`, `ค่าอุปกรณ์ไอที`, `ค่าเครื่องจักร`, `ค่าใช้จ่ายสวน` เข้าหมวดใหม่ได้ถูก, และให้ `getActiveQuickAddProfile()` refresh profile/สร้างหมวดใหม่ให้ผู้ใช้ที่เคยเปิด template ไปแล้วโดยไม่ต้องกดใช้ซ้ำ; `npx.cmd tsc --noEmit` ผ่าน, `node .\\tests\\quick-add\\run-quick-add-regression.cjs --suite all` ผ่าน 298/298 | `services/starterTemplateService.ts`, `services/quickAddParser.ts`, `tests/quick-add/fixtures/category-profiles.json`, `tests/quick-add/cases.th.json`, `tests/quick-add/cases.adversarial.th.json`, `PROGRESS-LOG.md` |
| 22 พฤษภาคม 2569 | Voice Input / Phase 1 | เริ่ม Voice Input แบบออฟไลน์ในแอปเองสำหรับ Android dev build | เสร็จแล้ว | เพิ่ม `voiceInputService` ฝั่ง JS, เพิ่ม native Android module/package สำหรับ `SpeechRecognizer` ที่รองรับ on-device recognition, partial/final results และ bias phrases, เพิ่มปุ่มไมค์/สถานะ Voice Input ในหน้า `Add`, และให้ transcript ไหลเข้า Quick Add โดยตรง; `npx.cmd tsc --noEmit` ผ่าน, ส่วน `gradlew app:compileDebugKotlin` ยังยืนยันไม่จบใน sandbox เพราะ Gradle ต้องดาวน์โหลด distribution ผ่านเครือข่าย | `services/voiceInputService.ts`, `app/(tabs)/add.tsx`, `android/app/src/main/java/com/sevendog/poatung/PoatungVoiceInputModule.kt`, `android/app/src/main/java/com/sevendog/poatung/PoatungVoiceInputPackage.kt`, `android/app/src/main/java/com/sevendog/poatung/MainApplication.kt`, `android/app/src/main/AndroidManifest.xml`, `PROGRESS-LOG.md` |
| 22 พฤษภาคม 2569 | Voice Input / Phase 1.1 | แก้เคส Android `error 13` และแยกสถานะ “ภาษาไทยออฟไลน์พร้อมใช้จริง” ออกจาก “มี recognizer ในเครื่อง” | เสร็จแล้ว | เพิ่ม locale-aware capability check ผ่าน `checkRecognitionSupport`, ให้ตรวจว่า `th-TH` ถูกติดตั้งจริงหรือยัง, เปลี่ยน badge/ข้อความในหน้า Add ไม่ให้ขึ้น `เสียงออฟไลน์พร้อมใช้` ผิด ๆ, และถ้ายังไม่มี model ไทยออฟไลน์ให้ขอดาวน์โหลด/แจ้งผู้ใช้ด้วยข้อความตรงขึ้น; `npx.cmd tsc --noEmit` ผ่าน และ `gradlew app:compileDebugKotlin --no-daemon --console=plain` ผ่านหลังย้าย `GRADLE_USER_HOME`/`ANDROID_USER_HOME` มาไว้ใน workspace | `android/app/src/main/java/com/sevendog/poatung/PoatungVoiceInputModule.kt`, `services/voiceInputService.ts`, `app/(tabs)/add.tsx`, `PROGRESS-LOG.md` |
| 23 พฤษภาคม 2569 | Voice Input / Pause Note | พักงานหลังแก้ Phase 1.1 และรอรัน Android build ใหม่บนเครื่องจริง | เสร็จแล้ว | ผู้ใช้ทดสอบเจอ `error 13` จากไมค์ในแอป, โค้ดแก้แล้วทั้ง native + UI + capability check, เหลืองานถัดไปคือ `npx expo run:android` เพื่ออัปเดตแอปบนมือถือ แล้วทดสอบว่าขึ้นข้อความ `ดาวน์โหลดภาษาไทยออฟไลน์`/`กำลังดาวน์โหลด`/`พร้อมใช้` ถูกต้อง และลองโหมดเครื่องบินซ้ำ | `android/app/src/main/java/com/sevendog/poatung/PoatungVoiceInputModule.kt`, `services/voiceInputService.ts`, `app/(tabs)/add.tsx`, `PROGRESS-LOG.md` |
| 23 พฤษภาคม 2569 | DX / Single-App Flow | ลดความสับสนระหว่าง Expo Go กับ Android development build ให้เหลือ flow เดียว | เสร็จแล้ว | เพิ่มหน้ากันการใช้งานใน Expo Go โดยตรง เพื่อบังคับให้เปิดผ่าน development build/แอปจริงเท่านั้น, เปลี่ยน script `start` ให้ใช้ `expo start --dev-client`, เปลี่ยนชื่อ Android app เป็น `หมูนุ่น-เป๋าตุง`, และ sync native splash/icon ของ Android ให้ใช้โลโก้แบรนด์แทน placeholder เดิม; `npx.cmd tsc --noEmit` ผ่าน และ `gradlew app:processDebugResources --no-daemon --console=plain` ผ่าน | `app/_layout.tsx`, `package.json`, `android/app/src/main/res/values/strings.xml`, `android/app/src/main/res/values/colors.xml`, `android/app/src/main/res/values/styles.xml`, `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`, `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml`, `android/app/src/main/res/drawable-nodpi/app_brand_logo.png`, `PROGRESS-LOG.md` |
| 23 พฤษภาคม 2569 | UX / Voice Pause + Splash Polish | ซ่อนไมค์ในแอปชั่วคราวและปรับ splash/logo ให้สะอาดขึ้นสำหรับการทดสอบจริง | เสร็จแล้ว | ปิดการแสดงปุ่ม Voice Input และแถบสถานะจากหน้าเพิ่มรายการ โดยคงโค้ดไว้หลัง feature flag เพื่อเปิดกลับได้ภายหลัง, เปลี่ยน splash background เป็นสีขาว, และสร้างโลโก้เวอร์ชัน padded สำหรับ splash/adaptive icon เพื่อลดอาการโลโก้ล้นหรือโดน crop บน Android หลายรุ่น; `npx.cmd tsc --noEmit` และ `gradlew app:processDebugResources --no-daemon --console=plain` เป็นขั้นยืนยันถัดไปหลัง build ใหม่ | `app/(tabs)/add.tsx`, `app.json`, `assets/logo/logo-moonoon-paotung-padded.png`, `android/app/src/main/res/drawable-nodpi/app_brand_logo_padded.png`, `android/app/src/main/res/values/colors.xml`, `android/app/src/main/res/values/styles.xml`, `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`, `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml`, `PROGRESS-LOG.md` |
| 23 พฤษภาคม 2569 | Web / Localhost QA | แก้เว็บ bundle ให้รันบน localhost:8081 ได้สำหรับการตรวจ UI และแชร์ screenshot จากคอม | เสร็จแล้ว | เพิ่ม Metro config ให้รองรับ `expo-sqlite` บนเว็บด้วย `.wasm` asset และ COEP/COOP headers สำหรับ SharedArrayBuffer, ยืนยัน `npx.cmd expo export --platform web` ผ่านจนสร้าง `dist`, และล็อก script `web` ให้รันที่พอร์ต `8081` โดยตรง | `metro.config.js`, `package.json`, `dist/`, `PROGRESS-LOG.md` |
| 23 พฤษภาคม 2569 | Web / Charts Compatibility | แก้ `react-native-gifted-charts` ให้เปิดบนเว็บได้โดยไม่ต้องติดตั้ง gradient package เพิ่ม | เสร็จแล้ว | เพิ่ม Metro alias เฉพาะ `platform=web` ให้ redirect `react-native-linear-gradient`/`expo-linear-gradient` ไปยัง shim ของโปรเจกต์ และสร้าง `web/shims/LinearGradient.tsx` เพื่อวาด CSS linear-gradient สำหรับงานตรวจ UI บนคอม; ยืนยัน `npx.cmd expo export --platform web` ผ่านอีกรอบหลังแก้ | `metro.config.js`, `web/shims/LinearGradient.tsx`, `dist/`, `PROGRESS-LOG.md` |
| 23 พฤษภาคม 2569 | Web / SQLite Runtime Stability | แก้ `Sync operation timeout` บนเว็บด้วยการเลื่อนการเปิดฐานข้อมูลออกจากช่วง import module | เสร็จแล้ว | เปลี่ยน `db/client.ts` จากการเรียก `openDatabaseSync()` แบบ top-level ไปเป็น lazy proxy สำหรับ `expoDb` และ `db` เพื่อไม่ให้ SSR/initial bundle เปิด SQLite เร็วเกินไปบน web runtime, และเก็บ warning `pointerEvents` ใน gradient shim ให้ log สะอาดขึ้น; `npx.cmd tsc --noEmit` ผ่าน และ `npx.cmd expo export --platform web` ผ่าน | `db/client.ts`, `web/shims/LinearGradient.tsx`, `dist/`, `PROGRESS-LOG.md` |
| 23 พฤษภาคม 2569 | Web / SecureStore Fallback | แก้ `ExpoSecureStore.default.getValueWithKeyAsync is not a function` บนเว็บด้วย storage fallback และ biometric guard | เสร็จแล้ว | เพิ่ม `appSecureStore` ให้ fallback ไป `localStorage`/memory บนเว็บแทน `expo-secure-store`, เปลี่ยน `authService` และ `notificationService` ให้เรียกผ่าน wrapper, และปิด biometric บนเว็บแบบ graceful เพื่อให้หน้า UI เปิดได้ต่อเนื่องระหว่างตรวจดีไซน์; `npx.cmd tsc --noEmit` ผ่าน และ `npx.cmd expo export --platform web` ผ่าน | `lib/secureStore.ts`, `services/authService.ts`, `services/notificationService.ts`, `PROGRESS-LOG.md` |
| 9 มิถุนายน 2569 | Smart Entry / Trade Set Categories | แก้ state หมวดหมู่ trade-set ไม่ให้ `type=income` ค้างกับหมวดฝั่งรายจ่าย | เสร็จแล้ว | เพิ่ม payload `tradeSet` ใน parser ให้มี `cost.category=ต้นทุนขาย`, `revenue.category`, และ `businessActivity` ชัดเจน; หน้า Add ใช้ payload นี้แทน keyword fallback ที่เคยเจอ `ธุรกิจส่วนตัว` ก่อน `ขายของในตลาด`; แก้ learning normalize ให้ match ข้อความหลังตัดยอดเงิน; เพิ่ม assertion ใน `test:market-intent` สำหรับ cost/revenue/activity และ learning normalize; ผ่าน `test:market-intent` 30/30, `test:quick-add:all` 298/298, `test:smart-parser`, `test:golden:check`; `npx tsc --noEmit` ยังติด error เดิมที่ `app/(tabs)/add.tsx:202` | `services/quickAddParser.ts`, `services/quickAddLearningService.ts`, `app/(tabs)/add.tsx`, `tests/market-intent/run-market-intent.cjs`, `tests/market-intent/cases.market-intent.json`, `PROGRESS-LOG.md` |
| 9 มิถุนายน 2569 | Smart Entry / Structural Trade Set | เพิ่มกฎ structural สำหรับประโยค `รับ...จ่ายไป...ขายได้` และกัน sign error `ขาย...ได้` | เสร็จแล้ว | ไม่เพิ่มคำว่า `รับ` เป็น synonym ของ `ซื้อ`; เพิ่ม cost-leg trigger แบบมี guard (`จ่ายไป/จ่ายเงินไป + จำนวน`) ร่วมกับ sale-leg (`ขาย...ได้ + จำนวน`) เพื่อจับเคส `รับผักสดจากชุมชนจ่ายไป 1,300 บาทไปขายในตลาดได้ 3,400 บาท`; เพิ่ม sign override ให้ `ขาย...ได้` เป็นรายรับใน single path; เพิ่ม regression 2 เคสใหม่; ผ่าน `test:market-intent` 32/32, `test:quick-add:all` 298/298, `test:smart-parser`, `test:golden:check`; `npx tsc --noEmit` ยังติด error เดิมที่ `app/(tabs)/add.tsx:202` | `services/quickAddParser.ts`, `tests/market-intent/cases.market-intent.json`, `PROGRESS-LOG.md` |
| 9 มิถุนายน 2569 | QA / Trade Set Real Device Gate | ขยาย regression และ checklist สำหรับรอบตรวจเครื่องจริง | เสร็จแล้ว | เพิ่ม assert ยอด `cost/revenue/profit` ใน market-intent runner, ขยาย mini golden set เป็น 44 เคส ครอบคลุมตลาดชัด, ธุรกิจส่วนตัวเมื่อไม่พูดตลาด, ขาดทุน, เท่าทุน/หลายยอดที่ต้องไม่ high, และ structural `จ่ายไป+ขายได้`; เพิ่ม checklist ตรวจหลังบันทึกจริง/ประวัติ/report/learning; ผ่าน `test:market-intent` 44/44, `test:quick-add:all` 298/298, `test:smart-parser`, `test:golden:check`; `npx tsc --noEmit` ยังติด error เดิมที่ `app/(tabs)/add.tsx:202` | `tests/market-intent/run-market-intent.cjs`, `tests/market-intent/cases.market-intent.json`, `tests/market-intent/tradeSetRealDeviceChecklist.md`, `PROGRESS-LOG.md` |
| 9 มิถุนายน 2569 | Milestone / Pro Core Handoff | ปิดรอบพัฒนา Pro Core และส่งต่อสู่ Premium AI Roadmap | เสร็จแล้ว | ผู้ใช้ทดสอบบนเครื่องจริงแล้วพบว่า trade set, การบันทึก, และประวัติรายการทำงานถูกต้อง; บันทึกมติว่า Pro Core / Daily Snapshot / Smart Entry / Trade Set เข้าสู่ scope freeze แก้เฉพาะ bug/regression/UX เล็กน้อย และงานถัดไปคือเตรียมข้อเสนอประชุมทีมเพื่อสรุป Road Map Premium Version AI โดยใช้ local deterministic core เป็นฐาน | `PREMIUM-AI-HANDOFF.md`, `SMART-ASSIST-ROADMAP.md`, `DEVELOPMENT-PLAN.md`, `PROGRESS-LOG.md` |

---

## Current Next Step

**Next Step:** นำ `PREMIUM-AI-HANDOFF.md` เข้า meeting ทีม เพื่อสรุป Road Map Premium Version AI และกำหนดขอบเขต implementation รอบแรก เช่น architecture, feature flags, AI review gating, consent/privacy และ metrics ก่อนเริ่มเชื่อม API จริง
